[package]
name = "viper"
version = "0.1.0"
authors = ["Prusti Devs <prusti_developers@sympa.ethz.ch>"]
edition = "2021"

[lib]
doctest = false # we have no doc tests

[dependencies]
duchess = { path = "/r/duchess" }
vir = { path = "../vir" }
viper-defs = { path = "../viper-defs" }
