[package]
name = "systest"
version = "0.1.0"
build = "build.rs"
edition = "2021"

[build-dependencies]
jni-gen = { path = ".." }
error-chain = "0.12"
env_logger = "0.9"
ureq = "2.1"
tempdir = "0.3"

[dependencies]
error-chain = "0.12"
jni = { version = "0.19", features = ["invocation"] }
