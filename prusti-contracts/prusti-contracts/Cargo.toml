[package]
name = "prusti-contracts"
version = "0.1.0"
authors = ["Vytautas Astrauskas <vastrauskas@gmail.com>"]
edition = "2021"

[dependencies]
prusti-contracts-internal = { path = "../prusti-contracts-internal" }

[dev-dependencies]
trybuild = "1.0"

[features]
# Are we being compiled by Prusti?
prusti = ["prusti-contracts-internal/prusti"]
