[package]
authors = ["Federico Poli <federpoli@gmail.com>"]
description = "Generator of Rust bindings to Java classes"
name = "jni-gen"
readme = "README.md"
version = "0.1.0"

[dependencies]
log = "0.3"
error-chain = "0.11.0"
jni = { version = "0.9.3", features = ["backtrace", "invocation"] }

[dev-dependencies]
env_logger = "0.4.3"

[badges]
travis-ci = { repository = "viperproject/jni-gen", branch = "master" }
