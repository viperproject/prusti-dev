[package]
authors = ["Federico Poli <federpoli@gmail.com>"]
description = "Translation of MIR into Viper"
name = "prusti-viper"
readme = "README.md"
version = "0.1.0"

[dependencies]
log = "0.3"
viper = { git = "https://github.com/viperproject/viper.git" }
prusti-interface = { git = "https://github.com/viperproject/prusti-interface.git" }

[dev-dependencies]
env_logger = "0.4.3"
prusti-interface = { git = "https://github.com/viperproject/prusti-interface.git" }

[badges]
travis-ci = { repository = "viperproject/prusti-viper", branch = "master" }
