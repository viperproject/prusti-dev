[package]
name = "overflow_checks"
version = "0.1.0"
edition = "2021"

[profile.dev]
overflow-checks = false

[profile.release]
overflow-checks = false

# Declare that this crate is not part of a workspace
[workspace]
