[package]
name = "library_contracts_lib"
version = "0.1.0"
edition = "2021"

[dependencies]
prusti-contracts = { path = "../prusti-contracts/prusti-contracts" } # The test suite will prepare a symbolic link for this
