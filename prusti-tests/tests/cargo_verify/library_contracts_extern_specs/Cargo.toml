[package]
name = "library_contracts_extern_specs"
version = "0.1.0"
edition = "2021"

[dependencies]
library_contracts_lib = { path = "../library_contracts_lib" }
prusti-contracts = { path = "prusti-contracts/prusti-contracts", features = ["prusti"] } # The test suite will prepare a symbolic link for this
