[package]
name = "failing_crate_fail"
version = "0.1.0"
edition = "2021"

[dependencies]
prusti-contracts = { path = "../../../../prusti-contracts/prusti-contracts" }

# Declare that this crate is not part of a workspace
[workspace]
