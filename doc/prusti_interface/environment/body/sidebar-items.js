window.SIDEBAR_ITEMS = {"struct":[["EnvBody","Store for all the `mir::Body` which we’ve taken out of the compiler or imported from external crates, all of which are indexed by DefId"],["MirBody","Stores any possible MIR body (from the compiler) that Prusti might want to work with. Cheap to clone"]]};