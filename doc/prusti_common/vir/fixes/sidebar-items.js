window.SIDEBAR_ITEMS = {"fn":[["fix_ghost_vars","Viper has a consistency check that only variables declared inside the package statement can be assigned in it. Since these ghost variables are generated by method calls that happen outside the package statement, it is hard to satisfy this requirement when creating the encoding. Therefore, we fix this with an additional pass that renames all variables declared inside package statements so that they are unique."]]};