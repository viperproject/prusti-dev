window.SIDEBAR_ITEMS = {"fn":[["sendfile","Copy up to `count` bytes to `out_fd` from `in_fd` starting at `offset`."],["sendfile64","Copy up to `count` bytes to `out_fd` from `in_fd` starting at `offset`."]]};